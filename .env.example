# Database
DATABASE_URL=postgresql://polaris:polaris@localhost:5432/polaris

# Direct-to-PostgreSQL URL for installs running PgBouncer in front of Postgres.
# When set, Polaris routes pg-boss, pg_dump backup/restore, and pg_stat_activity
# reads through this URL; Prisma still uses DATABASE_URL (PgBouncer can absorb
# the Prisma pool fine, but pg-boss and pg_dump need a direct session). Leave
# unset on direct-to-Postgres deployments.
# POLARIS_DB_DIRECT_URL=postgresql://polaris:polaris@localhost:5432/polaris

# Persistent-state directory. When set, .env / .setup-complete / data/backups /
# data/agents / public/uploads all live under this dir. Defaults to the
# application root (which is what the install scripts under deploy/ provision,
# so the default works out of the box for /opt/polaris installs). Set
# explicitly when the OS policy forbids state files under the app dir
# (SELinux/STIG-restricted /opt, read-only filesystem, separate state volume).
# Docker images pin this to /app/state at build time.
# POLARIS_STATE_DIR=

# Process role (multi-process deployment). Unset = "all" = single process runs
# everything (the default; existing installs and `npm run dev` are unchanged).
# To split the workload across processes, run one process per role:
#   web        — HTTP/HTTPS + all singleton schedulers + one-shot migrations
#   monitor    — pg-boss monitor-queue consumers (run N replicas)
#   discovery  — pg-boss discovery-queue consumer
# Set this per-process via the service unit / container env, NOT in this shared
# .env (every process reads the same .env). See docs/INSTALL.md → Multi-process.
# POLARIS_ROLE=all

# Cross-integration discovery parallelism for the discovery role (per-integration
# runs are serialized by the queue). Default 2.
# POLARIS_DISCOVERY_WORKERS=2

# Required on the web node in split-role mode: the number of polaris-monitor
# replicas in the deployment. The Capacity Advisor reads this to size both
# DATABASE_POOL_SIZE (per-process) and max_connections (group-wide) correctly;
# unset on a web-role process makes the advisor degrade to single-process math
# and the web role logs a startup warning. The shipped setup scripts persist
# this from --monitor-replicas automatically. Group footprint ≈ (replicas + 2)
# × (DATABASE_POOL_SIZE + POLARIS_PGBOSS_POOL_SIZE) must stay under Postgres
# max_connections (PgBouncer absorbs the Prisma pools but pg-boss pools always
# hit Postgres directly).
# POLARIS_MONITOR_REPLICAS=

# pg connection-pool size for the Prisma driver adapter. Default 25 — covers
# the monitor worker pool + HTTP handlers + background jobs at small/medium
# scale. Raise this when running with many monitored assets (rule of thumb:
# 2× expected concurrent monitor workers + 10–20 for HTTP). The DB itself
# must allow at least this many connections (Postgres `max_connections`).
# DATABASE_POOL_SIZE=25

# Monitor worker concurrency. Defaults are CPU-aware: light loop (probe +
# fastFiltered) uses cpus().length * 2 (clamped 8..64); heavy loop (telemetry
# + systemInfo) uses cpus().length (clamped 4..32). Override either when
# benchmarking shows the default is too generous (small VM, shared host) or
# too tight (many monitored assets, idle CPUs). Cursor-queue mode only.
# POLARIS_PROBE_CONCURRENCY=
# POLARIS_HEAVY_CONCURRENCY=

# pg-boss worker localConcurrency (only consulted when the monitor queue is
# in "pgboss" mode — flip via the Maintenance tab's [Enable on next restart]
# button). Architecture: 24 dedicated workers per queue (4 × 24 = 96 slots)
# plus a floating pool of 32 workers that polls all six queues in priority
# order (probe > fastFiltered > lldp > storage > telemetry > systemInfo).
# Raise these alongside DATABASE_POOL_SIZE. LLDP + Storage default to 12
# workers each because each walks only one MIB family (LLDP-MIB or
# HOST-RESOURCES-MIB hrStorageTable) and is much lighter than the full
# systemInfo pass.
# POLARIS_MONITOR_PROBE_WORKERS=24
# POLARIS_MONITOR_FAST_WORKERS=24
# POLARIS_MONITOR_HEAVY_WORKERS=24
# POLARIS_MONITOR_LLDP_WORKERS=12
# POLARIS_MONITOR_STORAGE_WORKERS=12
# POLARIS_MONITOR_FLOATING_WORKERS=32

# pg-boss internal connection pool size (pgboss mode only). pg-boss manages
# its own pg.Pool separate from Prisma's DATABASE_POOL_SIZE pool. Default 20
# — sized for the default worker count (max(16, min(64, cpus().length × 4))
# per queue). Raise alongside DATABASE_POOL_SIZE when connections peak near
# max_connections during monitoring-heavy or discovery-heavy periods.
# POLARIS_PGBOSS_POOL_SIZE=20

# Per-waiter wait-timeout (ms) for the per-SNMP-agent serialization gate in
# monitoringService.withSnmpGate. All probe / telemetry / systemInfo /
# fastFiltered SNMP calls against the same host:port FIFO-serialize through
# this gate to avoid stepping on net-snmp's single-conversation state. When
# the currently-running collector wedges (e.g. dead-host net-snmp 60s
# timeout), queued callers fail fast after this timeout instead of all
# waiting the full upstream duration; the wedged slot itself still holds
# the gate until it returns. Default 30000 — enough headroom for a heavy
# + telemetry back-to-back (~20s with default tier-3 timeouts), short
# enough to surface a hang. Operator snmp-walks (the asset SNMP Walk tab)
# override this per-call to 50s so a walk queued behind a monitoring poll
# can use the walk tab's full 60s client countdown window.
# POLARIS_SNMP_GATE_WAIT_TIMEOUT_MS=30000

# TimescaleDB compression window (days). When the timescaledb extension is
# installed, monitoring sample tables become hypertables and chunks older
# than this many days are compressed (typically 10-30× space reduction).
# Default 7 keeps the 1h / 24h / 7d UI chart ranges on uncompressed chunks
# for instant queries; bump to 14 or 30 if you want even faster recent-history
# charts at the cost of more disk, or drop to 1 to maximize disk savings on
# tight-disk hosts. 0 disables compression. Has no effect when the extension
# is not installed.
# TIMESCALE_COMPRESS_AFTER_DAYS=7

# App
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# Session secret — required when NODE_ENV=production (server refuses to boot
# without it). Generate a long random value, e.g. `openssl rand -hex 64`.
SESSION_SECRET=change-me-before-production

# Reverse-proxy trust — leave UNSET when the app is exposed directly to the
# internet. Setting this while not behind a proxy lets clients spoof their IP
# via X-Forwarded-For and bypass the login rate limiter. Enable only when
# running behind nginx/Caddy/Cloudflare/an ALB/etc:
#   TRUST_PROXY=1              # trust the nearest one hop
#   TRUST_PROXY=loopback       # trust 127.0.0.1 / ::1
#   TRUST_PROXY=10.0.0.0/8     # trust a specific CIDR
# See https://expressjs.com/en/guide/behind-proxies.html
# TRUST_PROXY=

# Health check token — when set, /health requires `Authorization: Bearer <token>`.
# The first-run setup wizard auto-generates this so the endpoint isn't
# exposed open out of the box. Generate a fresh value with
# `openssl rand -hex 32`. Clear the value if you genuinely need an open
# endpoint (e.g. some L4 health probes can't carry a header); the
# Maintenance tab will surface a warning while it is unset.
# HEALTH_TOKEN=

# Prometheus metrics token — when set, /metrics requires
# `Authorization: Bearer <token>`. Auto-generated by the first-run setup
# wizard because the endpoint leaks fleet size, monitor health by status,
# monitor pass duration, and queue depth as recon data — leaving it open
# on a public deployment is a meaningful information leak. Generate a
# fresh value with `openssl rand -hex 32`. Clear the value to expose
# the endpoint open (the Maintenance tab will surface a warning).
# METRICS_TOKEN=

# ─── Reverse-proxy (nginx) front-end ────────────────────────────────────────
# Polaris listens HTTP-only on 127.0.0.1; nginx terminates TLS on 443 using
# the cert at POLARIS_PROXY_CERT_PATH. The same fingerprint is what every
# Polaris Agent pins. See deploy/migrate-to-nginx.sh for the cutover script.
#
# Required (app refuses to boot otherwise — fail-fast). Must be an https://
# URL with a hostname; an explicit port is optional. Used by Polaris Agent
# installs to stamp server_url in agent.conf, and parsed by
# publicUrl.getPublicUrlPort() for the inferOwnServerUrl() chain.
# POLARIS_PUBLIC_URL=https://polaris.example.com

# Path to the leaf cert PEM file that nginx serves. Polaris reads this on
# demand to compute the fingerprint exposed at GET /server-settings/https.
# Use the SAME path nginx reads from — divergence causes agent pin drift on
# the next cert rotation. ACME / certbot deployments should point both nginx
# and this var at the certbot path directly.
# POLARIS_PROXY_CERT_PATH=/etc/polaris-nginx/cert.pem

# Standalone /metrics listener for non-HTTP roles in the multi-process split.
# Leave UNSET on single-process (POLARIS_ROLE unset / =all) and on the web
# role — those serve /metrics on the main HTTPS listener. Set per-process
# only when POLARIS_ROLE=monitor / discovery so Prometheus can scrape the
# work-duration / probe / sample-write / discovery histograms that live in
# each consumer's in-memory registry. Shipped systemd units default to 910N
# for monitor instance N and 9110 for discovery; same METRICS_TOKEN gates
# every endpoint. Set POLARIS_METRICS_BIND=0.0.0.0 if Prometheus runs off-host
# (default 127.0.0.1). Set this in the service unit / container env, NOT in
# this shared .env, because monitor replicas each need a unique port.
# POLARIS_METRICS_PORT=
# POLARIS_METRICS_BIND=

# Public URL the Polaris Agent should call back to. Stamped into
# /etc/polaris-agent/agent.conf (or %ProgramData%\Polaris\agent\agent.conf)
# when an operator installs the agent on a remote host. REQUIRED for any
# reverse-proxy / TLS-termination-upstream topology — the agent runs on a
# remote host that almost certainly can't reach the upstream Polaris's
# internal address, so without this it tries the upstream URL and fails
# the TLS handshake. Single-box installs default to https://localhost:<PORT>
# which is fine for testing but useless for any real install.
# POLARIS_PUBLIC_URL=https://polaris.example.com:3000

# Public hostname the agent should embed in agent.conf when POLARIS_PUBLIC_URL
# isn't set. Less general than POLARIS_PUBLIC_URL (no scheme, no port
# override), but covers the simple "just point agents at this hostname"
# case. POLARIS_PUBLIC_URL wins when both are set.
# POLARIS_PUBLIC_HOST=polaris.example.com

# How many old agent binary versions to retain under data/agents/ when the
# Server Settings → Maintenance → Polaris Agent → Clean up button (or the
# post-build auto-prune) runs. Counts directories NEWEST-FIRST by mtime,
# excludes the current manifest version + any version still in use by a
# live ManagedAgent. Default 3 = keeps two rollback targets behind the
# current one. Set 0 to delete every removable version on prune.
# POLARIS_AGENT_KEEP_VERSIONS=3

# Diagnostic: when set to "1", the web role logs one info line per inbound
# agent /samples push (stream, count, and the first telemetry sample's
# cpu/mem) plus a "telemetry enqueued" line. Only agents hit /samples, so the
# volume is one line per push per agent. Off by default — pair with the
# agent-side `verbose = true` in agent.conf to trace a telemetry round-trip
# end to end. Leave unset in normal operation.
# POLARIS_AGENT_SAMPLE_LOG=1

# Go toolchain selection for the in-app agent Build feature. Forwarded to
# `go build` via the GOTOOLCHAIN env var. Defaults to "local" which uses the
# host's installed Go. Set to "auto" to let Go download the version pinned
# in agent/go.mod on demand (useful when the host's Go is too old to compile
# the agent and you can't bump the host toolchain). Offline hosts must stay
# on "local".
# GOTOOLCHAIN=local

# Diagnostic: when set to "1" / "true", the ghost-merge job (which collapses
# duplicate hostname AssetSources with NULL MACs into the canonical asset)
# logs what it WOULD merge but performs no writes. Use when investigating
# unexpected merge behavior on a production install. Leave unset for normal
# operation.
# POLARIS_GHOST_MERGE_DRY_RUN=

# Notification outbound-channel secrets. Channel config (host/port/from/tenant/
# etc.) is set in the UI (Server Settings → Notifications); the SECRETS may
# instead live here (org Key-Vault/env pattern). When set, the env value
# overrides the stored (masked) Setting at send time.
# POLARIS_SMTP_PASSWORD=
# POLARIS_M365_CLIENT_SECRET=
# Web Push VAPID keypair — normally generated in the UI. Set here to pin a
# specific keypair (public + private must match). POLARIS_VAPID_SUBJECT is the
# mailto:/https: contact embedded in pushes.
# POLARIS_VAPID_PUBLIC_KEY=
# POLARIS_VAPID_PRIVATE_KEY=
# POLARIS_VAPID_SUBJECT=

# Git repository the in-app updater (Server Settings → Maintenance) fetches and
# pulls from. When SET, the URL is applied to the `origin` remote before every
# update check/apply — point installs at a fork or internal mirror here. When
# UNSET, the install's existing `origin` remote is left untouched (it updates
# from wherever it was cloned). The Application Updates card shows the active
# repo + whether it came from this var or the origin remote.
# POLARIS_UPDATE_REPO=https://github.com/rogers-group-inc/polaris.git
